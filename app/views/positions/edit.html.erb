<%= form_for(@position) do |f| %>
  <%= f.label "Name", for: :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<h1>Positions</h1>
<ul>
<% @positions.each do |position| %>
  <li><%= link_to(position.name, edit_position_path(position)) %></li>
<% end %>
</ul>
